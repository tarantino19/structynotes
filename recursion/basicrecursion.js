//common pattern - get first element, do something with it, call function again with rest of elements
//get first element - numbers[0]
//slice array
//return. base case as the number ti return


//if array - find base case - if (array.length === 0) return 0
//use .length if params/args i array
//else return first element + function again with rest of elements
//rest of elements - slice(1) - creates new array without first element
//each slice gets a new array - and then numbers[0] gets the first property of that new array
//the slice is the key here
//
